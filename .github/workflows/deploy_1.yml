name: Terraform con Docker

on:
  push:
    branches: [master, develop]
  pull_request:
    branches: [master]

  workflow_dispatch:
    inputs:
      env:
        description: "Ambiente (dev, staging, prod)"
        required: true
        default: "dev"
      action:
        description: "Acción (plan, apply, destroy)"
        required: true
        default: "plan"

jobs:
  terraform_deploy:
    runs-on: ubuntu-latest

    env:
      TERRAFORM_DIR: ./shared/terraform-dia22-devops  # ✅ Variable global

    defaults:
      run:
        working-directory: ${{ env.TERRAFORM_DIR }} # ✅ Directorio base para todos los pasos

    steps:
      - name: 📥 Checkout del repositorio
        uses: actions/checkout@v4

      - name: 🐳 Construir imagen Docker
        run: docker build -t terraform-runner .

      - name: 🚀 Ejecutar Terraform desde Docker
        run: docker run --rm terraform-runner ${{ github.event.inputs.env }} ${{ github.event.inputs.action }}

      - name: 📦 Subir artefactos generados
        uses: actions/upload-artifact@v4
        with:
          name: archivos-generados
          path: |
            ${{ env.TERRAFORM_DIR }}/README.md
            ${{ env.TERRAFORM_DIR }}/outputs/*

  notify_slack_discord:
    needs: terraform_deploy   # ⬅️ RELACIÓN DIRECTA: espera a que termine el job terraform
    if: always()              # ⬅️ Se ejecuta incluso si terraform falla
    uses: ./.github/workflows/notify.yml
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
    with:
      status: ${{ needs.terraform.result }}
      branch: ${{ github.ref_name }}
      actor: ${{ github.actor }}
      repo: "$(basename "${{ github.repository }}")"